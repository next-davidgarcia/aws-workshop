package util

// SERVER_PORT Server port
const SERVER_PORT = 4000

// API_ROOT_PATH REST API root path
const API_ROOT_PATH = "/blognext/api/v1/post/{postId}"

// POST_BR_ID Bad Request, Invalid Post ID
const POST_BR_ID = "Invalid Post ID"

// POST_NF_TAG Bad Request, Tag´s post not exists
const POST_NF_TAG = "Tag´s post not exists"

// TAG_BR_ID Bad Request, Invalid Tag ID
const TAG_BR_ID = "Invalid Tag ID"

// TAG_BR_NAME Bad Request, Empty tag name
const TAG_BR_NAME = "Empty Tag name"

// TAG_NF_DB Not Foud, Tag not exists
const TAG_NF_DB = "Tag not exists"

// TAG_BR_BODY Bad Request, Invalid Tag request body
const TAG_BR_BODY = "Invalid Tag request body"

// API_OK_SERVER OK, Request Successfully
const API_OK_REQUEST = "Successfully"

// API_SE_DB Internal Server, Error with DB
const API_SE_DB = "Database error ocurred, please try again later"
